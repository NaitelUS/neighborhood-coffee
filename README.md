# The Neighborhood Coffee – Order App

React + Vite + Tailwind order form.

## Run locally
npm install
npm run dev

## Build
npm run build

## Deploy
Use Netlify. Build command: `npm run build`, Publish dir: `dist`.
